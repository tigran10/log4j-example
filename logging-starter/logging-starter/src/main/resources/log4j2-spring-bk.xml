<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <!-- Define common properties that can be overridden by microservices -->
        <Property name="LOG_PATTERN">${springProperty:logging.pattern.console:%d{yyyy-MM-dd HH:mm:ss} - %highlight(%-5level) [%X{X-Trace-Token}] [%X{X-User-Id}] [%X{X-Correlation-Id}] %logger{36} - %msg%n}</Property>
        <Property name="JSON_LAYOUT_FIELDS">${springProperty:logging.json.layout-fields}</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
        <Console name="JsonConsole" target="SYSTEM_OUT">
            ${JSON_LAYOUT_FIELDS}
        </Console>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="JsonConsole"/>
        </Root>
    </Loggers>
</Configuration>
